# Sentry browser DSN from Project Settings > Client Keys (DSN).
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development

# Optional fallback HTTP endpoint that accepts structured frontend error events.
VITE_ERROR_REPORT_URL=
